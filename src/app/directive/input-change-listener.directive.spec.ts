import { InputChangeListenerDirective } from './input-change-listener.directive';

describe('InputChangeListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new InputChangeListenerDirective();
    expect(directive).toBeTruthy();
  });
});
